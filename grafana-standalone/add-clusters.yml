---
# =============================================================================
# OCP Monitoring — Dodaj aktualnie zalogowany klaster OCP do Grafany
# =============================================================================
# Uzycie:
#   1. Zaloguj sie do klastra:   oc login https://api.klaster.example.com ...
#   2. Odpal playbook:           ansible-playbook add-clusters.yml \
#                                  -i inventory/hosts.ini \
#                                  -e cluster_name=prod
#
# Playbook wygeneruje plik datasource na bastionie w katalogu ./generated/
# Nastepnie skopiuj go recznie na hosta Grafany (instrukcja na koncu).
# =============================================================================

- name: "Pobieranie danych z klastra OCP i generowanie datasource"
  hosts: bastion
  gather_facts: false
  vars:
    monitoring_namespace: openshift-monitoring
    validate_certs: false
    output_dir: "{{ playbook_dir }}/generated"

  tasks:

    - name: Sprawdzenie zmiennej cluster_name
      ansible.builtin.fail:
        msg: |
          Brak wymaganej zmiennej cluster_name.
          Uzycie: ansible-playbook add-clusters.yml -i inventory/hosts.ini -e cluster_name=prod
      when: cluster_name is not defined or cluster_name == ""

    - name: Sprawdzenie aktywnego logowania OCP (oc whoami)
      ansible.builtin.command:
        cmd: oc whoami
      register: oc_whoami
      changed_when: false
      failed_when: oc_whoami.rc != 0

    - name: Sprawdzenie serwera klastra (oc whoami --show-server)
      ansible.builtin.command:
        cmd: oc whoami --show-server
      register: oc_server
      changed_when: false

    - name: "Potwierdzenie kontekstu"
      ansible.builtin.debug:
        msg:
          - "Klaster   : {{ cluster_name }}"
          - "API URL   : {{ oc_server.stdout }}"
          - "Uzytkownik: {{ oc_whoami.stdout }}"

    # -------------------------------------------------------------------------
    # ServiceAccount + nie-wygasajacy token
    # -------------------------------------------------------------------------
    - name: Tworzenie ServiceAccount grafana-monitoring-reader
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: ServiceAccount
          metadata:
            name: grafana-monitoring-reader
            namespace: "{{ monitoring_namespace }}"
            labels:
              app: grafana-monitoring
              cluster: "{{ cluster_name }}"
              managed-by: ansible

    - name: Nadawanie ClusterRoleBinding cluster-monitoring-view
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: rbac.authorization.k8s.io/v1
          kind: ClusterRoleBinding
          metadata:
            name: grafana-monitoring-view
            labels:
              app: grafana-monitoring
              managed-by: ansible
          roleRef:
            apiGroup: rbac.authorization.k8s.io
            kind: ClusterRole
            name: cluster-monitoring-view
          subjects:
            - kind: ServiceAccount
              name: grafana-monitoring-reader
              namespace: "{{ monitoring_namespace }}"

    - name: Tworzenie nie-wygasajacego tokenu (SA Secret)
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: Secret
          metadata:
            name: grafana-monitoring-reader-token
            namespace: "{{ monitoring_namespace }}"
            annotations:
              kubernetes.io/service-account.name: grafana-monitoring-reader
            labels:
              app: grafana-monitoring
              managed-by: ansible
          type: kubernetes.io/service-account-token

    - name: Oczekiwanie na wypelnienie tokenu (max 60s)
      kubernetes.core.k8s_info:
        api_version: v1
        kind: Secret
        name: grafana-monitoring-reader-token
        namespace: "{{ monitoring_namespace }}"
      register: token_secret
      until:
        - token_secret.resources | length > 0
        - token_secret.resources[0].data is defined
        - token_secret.resources[0].data.token is defined
      retries: 12
      delay: 5

    # -------------------------------------------------------------------------
    # Thanos Querier Route
    # -------------------------------------------------------------------------
    - name: Pobieranie Route thanos-querier
      kubernetes.core.k8s_info:
        api_version: route.openshift.io/v1
        kind: Route
        name: thanos-querier
        namespace: "{{ monitoring_namespace }}"
      register: thanos_route

    # -------------------------------------------------------------------------
    # Test polaczenia
    # -------------------------------------------------------------------------
    - name: Test polaczenia z Thanos Querier
      ansible.builtin.uri:
        url: "https://{{ thanos_route.resources[0].spec.host }}/api/v1/query"
        method: POST
        headers:
          Authorization: "Bearer {{ token_secret.resources[0].data.token | b64decode }}"
          Content-Type: "application/x-www-form-urlencoded"
        body_format: form-urlencoded
        body:
          query: 'cluster_operator_conditions{condition="Available"}'
        validate_certs: "{{ validate_certs }}"
        status_code: 200
        timeout: 30
      register: test_result

    - name: "Test OK — operatorow w klastrze: {{ test_result.json.data.result | length }}"
      ansible.builtin.debug:
        msg: "Thanos Querier odpowiada. Operatorow: {{ test_result.json.data.result | length }}"

    # -------------------------------------------------------------------------
    # Generowanie pliku datasource lokalnie na bastionie
    # -------------------------------------------------------------------------
    - name: Tworzenie katalogu generated/
      ansible.builtin.file:
        path: "{{ output_dir }}"
        state: directory
        mode: '0700'

    - name: "Generowanie pliku datasource — generated/ocp-{{ cluster_name }}.yml"
      ansible.builtin.template:
        src: templates/datasource.yml.j2
        dest: "{{ output_dir }}/ocp-{{ cluster_name }}.yml"
        mode: '0600'
      vars:
        ds:
          name: "{{ cluster_name }}"
          thanos_url: "https://{{ thanos_route.resources[0].spec.host }}"
          bearer_token: "{{ token_secret.resources[0].data.token | b64decode }}"

    # -------------------------------------------------------------------------
    # Instrukcja co zrobic dalej
    # -------------------------------------------------------------------------
    - name: "============ GOTOWE — CO TERAZ ============"
      ansible.builtin.debug:
        msg:
          - "Plik datasource wygenerowany:"
          - "  {{ output_dir }}/ocp-{{ cluster_name }}.yml"
          - ""
          - "Skopiuj go na hosta Grafany:"
          - "  scp {{ output_dir }}/ocp-{{ cluster_name }}.yml \\"
          - "      marek@<grafana-host>:/tmp/ocp-{{ cluster_name }}.yml"
          - ""
          - "Na hoscie Grafany:"
          - "  sudo mv /tmp/ocp-{{ cluster_name }}.yml \\"
          - "          /etc/grafana/provisioning/datasources/ocp-{{ cluster_name }}.yml"
          - "  sudo chown grafana:grafana \\"
          - "          /etc/grafana/provisioning/datasources/ocp-{{ cluster_name }}.yml"
          - "  sudo systemctl reload grafana-server"
