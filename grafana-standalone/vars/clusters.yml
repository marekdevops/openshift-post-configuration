---
# =============================================================================
# Konfiguracja Grafany
# =============================================================================
grafana_port: 3000
grafana_admin_user: admin
grafana_admin_password: "OCP-Grafana-2025!"   # ZMIEN przed uruchomieniem
grafana_domain: "{{ ansible_fqdn }}"          # lub np. grafana.firma.pl

# =============================================================================
# Klastry OCP dodajesz przez:
#   oc login https://api.klaster.example.com ...
#   ansible-playbook add-clusters.yml -i inventory/hosts.ini -e cluster_name=prod --ask-become-pass
# =============================================================================
